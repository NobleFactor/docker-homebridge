services:
  homebridge:
    volumes:
      - ./volumes/US-WA:/homebridge
    extends:
      file: docker-compose.yaml
      service: homebridge
networks:
  external_network:
    name: ${NETWORK_NAME}
    external: true
