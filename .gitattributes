###############################################################################
# Repository attributes
#
# Note: git-crypt filters have been removed. Secrets and certificates are now
# excluded via .gitignore and generated locally during builds/tests.
# This file is intentionally minimal.
###############################################################################

# Ensure attributes file itself is always treated as plain text
.gitattributes -text
